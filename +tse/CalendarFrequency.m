classdef (Abstract) CalendarFrequency < tse.Frequency
    %CALENDARFREQUENCY Abstract intermediate type for calendar-aligned
    %frequencies (Daily, BDaily, Weekly, and all YPFrequency subclasses).
end
